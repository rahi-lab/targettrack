name: targettrack_server
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.8.13
  - numpy=1.23.4  # Pinned to match GUI
  - pytorch=2.1.0=py3.8_cuda12.1_cudnn8.9.2_0  # Pinned for CUDA compatibility
  - pytorch-cuda=12.1
  - torchvision=0.16.0  # Pinned to match PyTorch
  - scipy=1.10.1
  - scikit-image=0.21.0
  - scikit-learn=1.3.2
  - umap-learn=0.5.6
  - sparse=0.15.4
  - h5py=3.6.0  # Pinned to match GUI
  - hydra-core
  - pip
  - pip:
    - rpyc==5.3.1  # Pinned to match GUI
    - albumentations
    - alphashape
    - connected-components-3d